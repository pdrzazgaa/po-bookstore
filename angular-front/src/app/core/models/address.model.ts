export class Address {
  street: string;
  num: number;
  city: string;

  constructor(street: string, num: number, city: string) {
    this.street = street;
    this.num = num;
    this.city = city;
  }
}
